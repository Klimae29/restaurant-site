<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Effet parallax sur l'arrière-plan
    const heroBg = document.querySelector('.hero-bg');
    const heroContent = document.querySelector('.hero-content');

    window.addEventListener('scroll', () => {
      const scrolled = window.pageYOffset;
      const rate = scrolled * -0.5;
      const rate2 = scrolled * -0.2;

      if (heroBg) {
        heroBg.style.transform = `translateY(${rate}px)`;
      }
      if (heroContent) {
        heroContent.style.transform = `translateY(${rate2}px)`;
      }
    });

    // Animation des particules
    const particles = document.querySelectorAll('.particle');
    particles.forEach((particle, index) => {
      // Position aléatoire
      particle.style.left = Math.random() * 100 + '%';
      particle.style.top = Math.random() * 100 + '%';

      // Animation flottante avec keyframes CSS depuis globals.css
      particle.style.animationName = 'fadeInUp';
      particle.style.animationDuration = `${3 + Math.random() * 4}s`;
      particle.style.animationIterationCount = 'infinite';
      particle.style.animationTimingFunction = 'ease-in-out';
      particle.style.animationDelay = `${Math.random() * 2}s`;
    });

    // Observer pour les animations au scroll
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('animate-in');
        }
      });
    }, observerOptions);

    // Observer tous les éléments avec fade-in
    document.querySelectorAll('[class*="fade-in-delay"]').forEach(el => {
      observer.observe(el);
    });
  });
</script>---
// src/components/HeroSection.astro
---

<section class="hero-section relative min-h-screen flex items-end justify-center overflow-hidden">
  <!-- Image de fond avec effet parallax -->
  <div class="hero-bg absolute inset-0 z-0">
    <!-- Placeholder pour image de fond - remplacez par votre image -->
    <div class="absolute inset-0 bg-gradient-to-br from-red-700 via-orange-600 to-yellow-400"></div>
    <!-- Si vous avez une vraie image, remplacez le div ci-dessus par : -->
    <!-- <img src="/images/restaurant/hero-bg.jpg" alt="Restaurant background" class="w-full h-full object-cover"> -->

    <!-- Overlay dégradé pour lisibilité du texte -->
    <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent"></div>

    <!-- Particules décoratives animées -->
    <div class="particles absolute inset-0">
      <div class="particle absolute w-2 h-2 bg-yellow-400 rounded-full opacity-60"></div>
      <div class="particle absolute w-1 h-1 bg-white rounded-full opacity-40"></div>
      <div class="particle absolute w-3 h-3 bg-orange-600 rounded-full opacity-30"></div>
      <div class="particle absolute w-2 h-2 bg-yellow-400 rounded-full opacity-50"></div>
      <div class="particle absolute w-1 h-1 bg-white rounded-full opacity-60"></div>
      <div class="particle absolute w-2 h-2 bg-yellow-400 rounded-full opacity-40"></div>
    </div>
  </div>

  <!-- Motif décoratif géométrique -->
  <div class="absolute inset-0 z-10 opacity-20 pointer-events-none">
    <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">
      <defs>
        <pattern id="geometric" patternUnits="userSpaceOnUse" width="20" height="20">
          <path d="M 0 0 L 10 10 M 10 0 L 0 10 M 20 0 L 20 20 M 0 20 L 20 20"
                stroke="currentColor" stroke-width="0.5" fill="none"/>
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#geometric)" class="text-yellow-400"/>
    </svg>
  </div>

  <!-- Contenu principal -->
  <div class="hero-content relative z-20 text-center pb-20 px-4 max-w-6xl mx-auto">

    <!-- Badge "Nouveau" -->
    <div class="mb-8 fade-in-delay-1">
      <span class="inline-flex items-center gap-2 bg-yellow-400/90 text-gray-800 px-4 py-2 rounded-full text-sm font-semibold backdrop-blur-sm hover:bg-yellow-400 transition-all duration-300 hover:scale-105 transform">
        ✨ Nouveau à Paris
      </span>
    </div>

    <!-- Titre principal avec effet typewriter -->
    <h1 class="hero-title mb-8">
      <span class="font-['Dancing_Script'] text-6xl md:text-8xl lg:text-9xl font-bold text-yellow-400 drop-shadow-2xl block leading-none mb-4">
        ¡Bienvenidos!
      </span>
      <span class="font-['Playfair_Display'] text-2xl md:text-4xl lg:text-5xl text-white font-light tracking-wide block">
        au cœur de l'Amérique du Sud
      </span>
    </h1>

    <!-- Sous-titre descriptif -->
    <div class="hero-subtitle mb-12 max-w-4xl mx-auto">
      <p class="font-sans text-xl md:text-2xl lg:text-3xl text-white/90 leading-relaxed mb-6 fade-in-delay-3">
        Savourez l'âme de l'Amérique du Sud dans chaque bouchée
      </p>
      <p class="font-sans text-lg md:text-xl text-white/80 leading-relaxed fade-in-delay-4">
        Asado argentin • Tacos authentiques • Caipirinhas premium • Ambiance festive
      </p>
    </div>

    <!-- Boutons d'action -->
    <div class="hero-buttons flex flex-col sm:flex-row gap-6 justify-center items-center mb-16 fade-in-delay-5">
      <a href="#menu" class="btn-hero-primary group relative overflow-hidden">
        <span class="relative z-10 flex items-center gap-3">
          📖 Découvrir la Carte
        </span>
        <div class="absolute inset-0 bg-gradient-to-r from-orange-600 to-yellow-400 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-500 origin-left"></div>
      </a>

      <a href="#reservar" class="btn-hero-secondary group relative overflow-hidden">
        <span class="relative z-10 flex items-center gap-3">
          🎉 Réserver une Table
        </span>
        <div class="absolute inset-0 bg-white transform scale-x-0 group-hover:scale-x-100 transition-transform duration-500 origin-left"></div>
      </a>
    </div>

    <!-- Indicateurs sociaux -->
    <div class="hero-social flex justify-center items-center gap-8 text-white/60 fade-in-delay-6">
      <div class="text-center group cursor-pointer hover:text-yellow-400 transition-colors duration-300">
        <div class="text-3xl mb-2 group-hover:scale-110 transform transition-transform duration-300">⭐</div>
        <div class="text-sm">4.8/5</div>
        <div class="text-xs">Google</div>
      </div>
      <div class="text-center group cursor-pointer hover:text-yellow-400 transition-colors duration-300">
        <div class="text-3xl mb-2 group-hover:scale-110 transform transition-transform duration-300">🏆</div>
        <div class="text-sm">Primé</div>
        <div class="text-xs">2024</div>
      </div>
      <div class="text-center group cursor-pointer hover:text-yellow-400 transition-colors duration-300">
        <div class="text-3xl mb-2 group-hover:scale-110 transform transition-transform duration-300">❤️</div>
        <div class="text-sm">1000+</div>
        <div class="text-xs">Clients</div>
      </div>
    </div>
  </div>

  <!-- Flèche animée pour inviter au scroll -->
  <div class="scroll-indicator absolute bottom-8 left-1/2 transform -translate-x-1/2 z-30 animate-bounce">
    <div class="w-6 h-10 border-2 border-white/50 rounded-full flex justify-center">
      <div class="w-1 h-3 bg-white/70 rounded-full mt-2 animate-pulse"></div>
    </div>
    <p class="text-white/60 text-xs mt-2 text-center">Scroll</p>
  </div>
</section>

<style>
  .hero-section {
    min-height: 100vh;
    background-attachment: fixed;
  }

  /* Styles des boutons hero */
  .btn-hero-primary {
    @apply inline-flex items-center justify-center px-8 py-4 font-bold text-lg rounded-full;
    @apply hover:shadow-2xl transform hover:scale-105 transition-all duration-500;
    box-shadow: 0 10px 30px rgba(255, 210, 63, 0.3);
  }

  .btn-hero-secondary {
    @apply inline-flex items-center justify-center px-8 py-4 border-2 border-white text-white font-bold text-lg rounded-full;
    @apply hover:shadow-2xl transform hover:scale-105 transition-all duration-500;
  }

  /* Animations personnalisées */
  .fade-in-delay-1 { animation: fadeInUp 0.8s ease-out 0.2s both; }
  .fade-in-delay-2 { animation: fadeInUp 0.8s ease-out 0.4s both; }
  .fade-in-delay-3 { animation: fadeInUp 0.8s ease-out 0.6s both; }
  .fade-in-delay-4 { animation: fadeInUp 0.8s ease-out 0.8s both; }
  .fade-in-delay-5 { animation: fadeInUp 0.8s ease-out 1.0s both; }
  .fade-in-delay-6 { animation: fadeInUp 0.8s ease-out 1.2s both; }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(50px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(180deg); }
  }

  /* Animation de pulsation pour les éléments sociaux */
  .hero-social > div:hover {
    animation: pulse 1s infinite;
  }

  @keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.1); }
    100% { transform: scale(1); }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-title span:first-child {
      font-size: 4rem;
      line-height: 0.9;
    }

    .hero-subtitle p:first-child {
      font-size: 1.25rem;
    }

    .btn-hero-primary, .btn-hero-secondary {
      width: 100%;
      max-width: 300px;
    }
  }

  @media (max-width: 480px) {
    .hero-title span:first-child {
      font-size: 3rem;
    }

    .hero-content {
      padding-bottom: 3rem;
    }
  }
</style>
